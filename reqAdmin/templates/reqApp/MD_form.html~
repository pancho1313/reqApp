<form action="" method="post">
	{% csrf_token %}
    {% if identificador %}
        <input type="hidden" name="identificador" value={{ identificador }}>
    {% endif %}
    <div>
        {% if  textoIdentificador %}
            <b>{{ textoIdentificador }}</b>
        {% endif %}
        <div>Nombre: {{ form.nombre }}</div>
        <div class="top">Descripción:</div> <div class="top">{{ form.descripcion }}</div>
        <div><div class="top">RS asociados: </div><div class="top">{{ form.requisitosSoftware }}</div>(presionar "Ctrl" para seleccionar)</div>
        <div>Prioridad: {{ form.prioridad }}</div>
        <div>Costo: {{ form.costo }}</div>
    </div>
	<button type="submit">
		Guardar
	</button>
</form>
